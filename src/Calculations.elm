module Calculations exposing (add, subtract, multiply, divide)

add : Float -> Float -> Float
add x y =
  x + y

subtract : Float -> Float -> Float
subtract x y =
  x - y

multiply : Float -> Float -> Float
multiply x y =
  x * y

divide : Float -> Float -> Float
divide x y =
  x / y